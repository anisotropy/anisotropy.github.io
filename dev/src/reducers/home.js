import update from 'immutability-helper';

const initialState = {
	content: ''
};

const home = (state = initialState, action) => {
	switch(action.type){
		default:
			return state;
	}
};

export default home;
